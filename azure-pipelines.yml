# Starter pipeline
# Start with a minimal pipeline that you can customize to build and deploy your code.
# Add steps that build, run tests, deploy, and more:
# https://aka.ms/yaml

trigger:
- main

pool:
   vmImage: 'ubuntu-latest'

stages:
  - stage: Build
    jobs:
    - job: First
      steps:
      - bash: echo $(m2_home)
      - bash: echo $(Build.BuildNumber)
      - bash: echo $(Build.BuildId)
      - bash: echo $(Build.SourceBranchName)

  - stage: Deploy
    jobs:
    - job: Second
      steps:
      - bash: echo "in second job deploy"
  - stage: Scan
    variables:
     ant_home: /home/lijin/ant_home
    jobs:
    - job: third
      steps:
      - bash: echo "Scanning docker images"
      - bash: echo $(ant_home)
